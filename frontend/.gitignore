# Dependencies
node_modules/
/.pnp
.pnp.js

# Production build
/build
build/
dist/

# Env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Lockfile (intentionally not committed for this sample)
package-lock.json

# runtime-config.json is generated by the foundation deploy
# (foundation/scripts/write-frontend-config.mjs). A placeholder is committed so
# the app builds; the deploy overwrites it locally with your stack's values.
